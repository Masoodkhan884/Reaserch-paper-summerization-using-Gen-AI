# 📄 AI Research Paper Summarizer

A Streamlit app powered by Gemini 2.0 Flash that summarizes research papers by title and allows interactive chat about them.

## 🚀 Run Locally
```bash
pip install -r requirements.txt
streamlit run app.py

---

### 🧱 4. Initialize Git Repository

Open your project folder in **VS Code** or **terminal**, then run:

```bash
git init
git add .
git commit -m "Initial commit - AI Research Paper Summarizer"
